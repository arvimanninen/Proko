@model IEnumerable<Back.Models.QuestionScale>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ScaleMin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ScaleMax)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ScaleMin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ScaleMax)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.QuestionScaleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.QuestionScaleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.QuestionScaleID })
        </td>
    </tr>
}

</table>
