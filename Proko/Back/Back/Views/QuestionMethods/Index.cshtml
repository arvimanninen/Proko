@model IEnumerable<Back.Models.QuestionMethod>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QuestionScale.QuestionScaleID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Value)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QuestionScale.QuestionScaleID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Value)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.QuestionMethodID }) |
            @Html.ActionLink("Details", "Details", new { id=item.QuestionMethodID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.QuestionMethodID })
        </td>
    </tr>
}

</table>
